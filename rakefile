desc 'test all'
task :tests do
  Dir.glob 'test/test_*.rb' do |f|
    puts `ruby "#{f}"`
  end
end

desc 'compare performance with treetop'
task :bench do
  require "fileutils"
  FileUtils.cd 'bench'
  puts `ruby bench.rb`
end

