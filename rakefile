desc 'test all'
task :tests do
  Dir.glob "./test/test_*.rb" do |f|
    require f
  end
end

# alias
desc 'test all'
task :test => :tests

desc 'compare performance with treetop'
task :bench do
  require './bench/bench.rb'
end

